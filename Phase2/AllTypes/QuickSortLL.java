package Phase2.AllTypes;

import Phase2.LinkedList.LinkedListNode;

public class QuickSortLL {

    public static void swapNodeData(LinkedListNode a, LinkedListNode b) {
        int aData = a.data;
        int bData = b.data;
        a.data = bData;
        b.data = aData;
    }

    public static LinkedListNode quickSortLL(LinkedListNode head) {
        // Write your code here.

        return head;
    }
}
